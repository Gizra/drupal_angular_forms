<?php

/**
 * @file
 * Pipe restful module.
 */

/**
 * Implements hook_ctools_plugin_directory().
 */
function drupal_angular_restful_ctools_plugin_directory($module, $plugin) {
  if ($module == 'restful') {
    return 'plugins/' . $plugin;
  }
}
